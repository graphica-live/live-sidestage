name: live_sidestage_mobile
description: "LIVE Sidestage — LIVE Sidestage Analyticsの公式モバイルクライアント。"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+2

environment:
  sdk: ^3.13.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # 日付・時刻ピッカー(showDatePicker/showTimePicker)を日本語表示にするために要る。
  # SDKパッケージなのでバージョン指定不要(flutter SDKと同期する)。
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Card Deck方向のリブランディングで使う等幅書体(IBM Plex Mono)。TTFは同梱せず
  # 初回起動時にネットワークから取得してキャッシュする(main.dart参照)。
  google_fonts: ^6.2.1
  http: ^1.2.2
  socket_io_client: ^3.0.2
  flutter_secure_storage: ^9.2.4
  provider: ^6.1.2
  google_sign_in: ^6.2.2
  # Android にはネイティブの Apple 認証が無いので Custom Tab の web フローになる。
  # AndroidManifest への SignInWithAppleCallback Activity 追加と、MainActivity が
  # singleTask / singleTop であることが前提（このアプリは singleTop）。
  sign_in_with_apple: ^8.1.0
  voicevox_core: ^1.0.0
  audioplayers: ^6.0.0
  path_provider: ^2.1.5
  flutter_foreground_task: ^9.0.0
  # 11.x へ上げないこと。11.x の android/build.gradle は AGP 9 以上を検出すると
  # Kotlin Gradle Plugin の適用をスキップし、AGP 側の built-in Kotlin に任せる作りに
  # なっている。このプロジェクトは AGP 9.1.0 かつ android.builtInKotlin=false なので
  # プラグインの .kt が一切コンパイルされず、GeneratedPluginRegistrant.java が
  # FilePickerPlugin を解決できずにビルドが落ちる。
  # builtInKotlin=true にすると今度は KGP を無条件に適用する flutter_foreground_task が壊れる。
  # 10.3.x は KGP を無条件適用し compileSdk もアプリ側へ追従するので影響を受けない。
  # 10.3.11 は pub 上で retracted 済みなので 10.3.10 を上限にする。
  file_picker: 10.3.10
  # 効果音ラボ / MyInstants の検索画面から、元サイトを外部ブラウザで開くためだけに使う。
  # Android 11 以降は AndroidManifest の <queries> に https の VIEW intent が無いと
  # 起動先を解決できないので、そちらもセットで必要。
  url_launcher: ^6.3.1
  # 起動時のバージョン取得(強制アップデート判定・X-App-Versionヘッダー)に使う。
  package_info_plus: ^8.1.2
  marionette_flutter: ^0.6.0
  # Google Play課金(サブスクリプション)。Android専用に in_app_purchase_android を
  # 明示追加している(iOSは今回対象外、既存のApple課金APIへは未接続)。
  in_app_purchase: ^3.2.0
  # Billing Library 7系は2026-08-31でGoogle Playへの新規提出期限切れ(延長申請の期限が
  # 2026-11-01)。0.5.0でBilling Library 8.0.0へ上がっている(実装後レビュー指摘、HIGH)。
  in_app_purchase_android: ^0.5.0
  # Apple IAP(StoreKit2)。in_app_purchase本体はfederated pluginで、iOS実装はこのパッケージが
  # 別途要る(Android版のin_app_purchase_androidと対称)。
  in_app_purchase_storekit: ^0.4.11

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Apple サインインの「Custom Tab を閉じたら打ち切る」タイマーを実時間を待たずに検証する。
  # flutter_test の推移的依存だが、直接 import するので明示する。
  fake_async: ^1.3.1

  # AppConfigStore の検証用。flutter_foreground_task の推移的依存だが、
  # 保存先(SharedPreferences)のモックと、サービス稼働状態を差し替えるための
  # プラットフォーム実装をテストから直接触るので明示する。
  shared_preferences: ^2.3.2
  plugin_platform_interface: ^2.1.8

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/open_jtalk_dic_utf_8-1.11/
    - assets/voicevox_models/
    # ギフト名の日本語辞書は同梱しない。TikTok 公式の日本語名をサーバー
    # (GET /api/mobile/gifts の labelJa) から取って端末へ貯める。lib/core/gift_name_ja.dart 参照

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
