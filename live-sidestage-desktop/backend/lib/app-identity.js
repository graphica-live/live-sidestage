'use strict';

/** LocalAppData フォルダ名。既存インストール互換のため変えない。 */
const APP_NAME = 'TikEffect';
/** ユーザーに見せるアプリ名。 */
const DISPLAY_NAME = 'Live Sidestage Desktop';

module.exports = { APP_NAME, DISPLAY_NAME };
