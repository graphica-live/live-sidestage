[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma db push && npx next start -H 0.0.0.0 -p 3000"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
